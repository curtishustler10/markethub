{
  "name": "markethub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "check-markets": "node check_markets_status.js",
    "populate-markets": "node populate_sample_markets.js"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "dependencies": {
    "@radix-ui/react-dropdown-menu": "^2.1.16",
    "mapbox-gl": "^3.14.0"
  }
}
